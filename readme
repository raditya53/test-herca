test-kerja server
-npm init
-npm install express
-npm install mysql2
-npm install cors
node server.js untuk menjalankan server

test-kerja ui
-npm install
npm run dev untuk melihat tabel

untuk database, tinggal import saja sudah termasuk create DB dan table.

untuk postman bisa langsung di import dan langsung di coba dengan parameter yang disediakan
